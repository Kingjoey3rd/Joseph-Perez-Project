#include <iostream>

using namespace std;

int main()
{
    int count = 20;

    // Loop to count down from 20 to 1
    do {
        cout << count << ", ";
        --count;
    } while (count > 0);
    
    // Print final message
    cout << "Done!!!!!" << endl;

    return 0;
}